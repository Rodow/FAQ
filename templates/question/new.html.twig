{% extends 'base.html.twig' %}

{% block title %}{{parent()}} Poser Une Question {% endblock %}

{% block body %}

{# formulaire de réponse dispo pour les user connectés #}
<h4>Poser une nouvelle question : </h4>

    <div class="card-body">
        {{ form_start(questionForm)}} 
        {{ form_widget(questionForm)}} 
        <button class="btn btn-primary" type="submit">Poser une question</button>
        {{ form_end(questionForm)}} 
    </div>
</article>

{% endblock %}
